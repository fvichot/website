# The URL the site will be built for
base_url = "https://flo.id.au"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

theme = "serene"
minify_html = false
generate_feeds = false
feed_filenames = ["feed.xml"]

taxonomies = [{ name = "tags" }]

[markdown]
github_alerts = true  # optional, enables GitHub-style callout syntax

render_emoji = false
external_links_target_blank = false
external_links_no_follow = true
external_links_no_referrer = true
smart_punctuation = false

[markdown.highlighting]
style = "class"
light_theme = "github-light"  # or any zola built-in theme
dark_theme = "github-dark"    # or any zola built-in theme

[slugify]
paths = "on"
taxonomies = "on"
anchors = "on"

[extra]
sections = [
  { name = "blog", path = "/blog", is_external = false },
  { name = "projects", path = "/projects/battery_simulator.html", is_external = false },
  # { name = "github", path = "https://github.com/<your-username>", is_external = true },
]
blog_section_path = "/blog"

back_link_text = "Back" # Text of the back button
force_theme = false # false | "light" | "dark"

footer_copyright = "Content under CC BY-NC-SA"
footer_credits = true # Whether to show "Built with zola and serene" in footer

not_found_error_text = "404 Not Found"
not_found_recover_text = "« back to home"

reaction = false